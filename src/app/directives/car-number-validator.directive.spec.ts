import { CarNumberValidatorDirective } from './car-number-validator.directive';

describe('CarNumberValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CarNumberValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
